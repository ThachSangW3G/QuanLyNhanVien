import 'package:flutter/material.dart';

class BonusTab extends StatelessWidget {
  const BonusTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(
            color: Colors.blueAccent,
            child: const Center(
              child: Text("ConstactTab"),
            )));
  }
}
